export interface LoopMode {
  dataIcon: string
  dataInline: boolean
  isShow: boolean
  title: string
}
export interface SongList {
  name: string
  active: boolean
  icon?: string
}
export interface Options {
  id: string
  name: string
}
